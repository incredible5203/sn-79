release_id=flux-1.0.0
agent=FluxPrimeAgent
profile=flux
git_commit=local
bundled_at=2026-06-10T08:00:00Z
notes=Replace VaultPrimeAgent — custom vault_engine bled PnL; flux uses proven ascend_score_tick
design:
  - ascend_score_tick engine (same as improving UIDs 10/65/158)
  - flux profile: surge throughput + forge completion edge (6.5 ticks)
  - touch-join at 4+ ticks only (not 3.5 — less adverse selection)
  - tight inventory: 0.2% soft / 0.4% hard with cross flatten
  - 11 books/tick, 28 instructions, 12x8 rotation
  - cancel_all_on_startup clears bad resting orders from prior vault deploy
targets: match UID 10/65 trajectory (κ→0+, rising RT vol, placement top-100)
files:
  FluxPrimeAgent.py
  _ascend_agent_base.py
  competitive_utils.py
