release_id=vault-1.2.0
agent=VaultPrimeAgent
profile=balanced
git_commit=local
bundled_at=2026-06-10T03:30:00Z
notes=Balanced vault — always-on quoting with PnL guardrails (fixes v1.1 NO INSTRUCTIONS)
design:
  - Bootstrap fallback guarantees at least one order per tick
  - Moderate spread gates (3-4 ticks) vs v1.1 ascend-delegate (7-8.5)
  - Two-sided only on wide books (>=8 ticks) with RT edge check
  - Completion legs require 4+ tick edge vs fill price
  - Tight inventory flatten (0.4%/0.9%) without risk_off shutdown
  - Microprice bias with rotation fallback when edge unclear
  - Stale order cancels every tick
targets: κ growth, penalty=0, positive realized PnL, never empty ticks
files:
  VaultPrimeAgent.py
  _vault_agent_base.py
  vault_engine.py
  competitive_utils.py
